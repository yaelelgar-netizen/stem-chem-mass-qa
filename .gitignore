# dev-only preview (not part of the deployed lomda)
dev_index.html

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*~

# dev-only preview server config
.claude/

# standalone web build of the rock-drop applet; nothing in the lomda references it
applets/gravity-rock-drop-web/
